block-color ?= primary
alt-color ?= secondary

dialog
	border 2px solid alt-color
	display none
	&[open]
		display block
		position absolute
		max-width: 600px;
		margin: auto;
		top 0
		bottom 0
		left 0
		min-width 320px
	&::backdrop
		background-color rgba(lighten(alt-color, 60%), 0.8)